name = "career-predictor-worker"
main = "src/index.ts"
compatibility_date = "2024-01-01"

# Bind AI model
[ai]
binding = "AI"

# For local development, create a .dev.vars file with your keys
# EXA_API_KEY is stored as a secret (use wrangler secret put EXA_API_KEY)
# Websets are created dynamically for each user request
